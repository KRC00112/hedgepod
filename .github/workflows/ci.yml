name: ci

on:
  push:
    branches:
      - ci-experiment
  pull_request:
    branches:
      - ci-experiment

jobs:
  smart contracts:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repository
        uses: actions/checkout@v6


