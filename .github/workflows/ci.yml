name: ci

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  smart contracts:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repository
        uses: actions/checkout@v6


